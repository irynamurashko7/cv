document.addEventListener('DOMContentLoaded', () => {
  // ================== Skill cards =====================
  // Custom column-based filtering lives in the skills.html partial itself.
  // Filterizr is intentionally NOT used here anymore, since it manages
  // item positioning itself and overrides manual column control.
})
